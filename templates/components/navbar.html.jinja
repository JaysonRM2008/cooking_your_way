<!-- Left Sidebar Navigation -->
<nav class="fixed top-0 left-0 h-full w-64 bg-white shadow-md border-r border-gray-200 z-20">
  <div class="flex flex-col h-full p-6">

    <!-- User Info -->
    {% if current_user.is_authenticated %}
      <p class="mb-6 text-gray-800 font-semibold">
        Hi {{ current_user.name }}
      </p>
    {% endif %}

    <!-- Navigation Links -->
    <ul class="flex flex-col space-y-6 text-lg">

      <li>
        <a href="/" class="text-gray-600 hover:text-blue-600 font-medium transition">
          Home
        </a>
      </li>

      <li>
        <a href="/browse" class="text-gray-600 hover:text-blue-600 font-medium transition">
          Browse
        </a>
      </li>

      {% if not current_user.is_authenticated %}
        <li>
          <a href="/login" class="text-gray-600 hover:text-blue-600 font-medium transition">
            Login
          </a>
        </li>
      {% else %}
        <li>
          <a href="/logout" class="text-gray-600 hover:text-blue-600 font-medium transition">
            Logout
          </a>
        </li>
      {% endif %}

      <li>
        <a href="/cart" class="text-gray-600 hover:text-blue-600 font-medium transition">
          Cart
        </a>
      </li>

      <li>
        <a href="/conatct" class="text-gray-600 hover:text-blue-600 font-medium transition">
          Conatct
        </a>
      </li>

    </ul>
  </div>
</nav>